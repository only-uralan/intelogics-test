export const apps = (state) => state.applications.defaultCoordinates;

export const routeCoordinates = (state) => state.applications.routeCoordinates;

export const markerCoordinates = (state) =>
  state.applications.markerCoordinates;
